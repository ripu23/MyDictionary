<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:alpha="0.6"
    tools:context="com.example.ripu.mydictionary.MainActivity">

    <Button
        android:text="@string/search"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/searchBox"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="74dp"
        android:id="@+id/searchButton"
        android:onClick="searchWord" />

    <AutoCompleteTextView
        android:text="@string/searchBoxText"
        android:layout_height="wrap_content"
        android:background="@color/searchBoxColor"
        android:id="@+id/searchBox"
        android:drawableStart="@android:drawable/ic_menu_search"
        android:layout_width="280dp"
        tools:ignore="HardcodedText"
        android:inputType="textAutoComplete"
        android:textIsSelectable="false"
        android:textCursorDrawable="@android:drawable/ic_search_category_default"
        android:layout_marginTop="28dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:onClick="searchBoxClicked" />
</RelativeLayout>
